### Understanding `async and await` functions through this very little project using axios

For running locally ,install axios using:
`npm i --save axios`

After setting up run the command below to run the program:
`node index.js`
